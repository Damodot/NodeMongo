<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
    <title>Edit Todo List</title>
    <style>
        body {
            background-color: #343a40;
        }

        main {
            background-color: #212529;
            max-height: 80vh;
            overflow-y: auto;
            position: relative;
            width: 50%;
            margin: auto;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        button:active {
            transform: scale(0.9);
            transition: transform 0.5s ease;
        }

        @media (max-width: 767px) {
            main {
                width: 85%;
            }
        }
    </style>
</head>

<body class="d-flex justify-content-center align-items-center vh-100">
    <main>
        <form action="/user/update/<%= newTask._id %>" method="post" class="mb-4">
            <div class="header">
                <h1 class="text-center fw-bolder text-white">Edit Todo List</h1>
                <div class="input-group mt-4 w-100">
                    <input type="text" placeholder="Add Reminder" value="<%= newTask.task %>" name="task" class="form-control w-100 rounded-3">
                    <button class="btn btn-info mt-4 rounded-3" type="submit">Update Reminder</button>
                </div>
            </div>
        </form>
    </main>
</body>

</html>
